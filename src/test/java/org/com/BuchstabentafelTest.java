package org.com;

import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertEquals;

public class BuchstabentafelTest {
    /*
    @Test
    void testAddSingleWord() {
        // Arrange
        Buchstabentafel buchstabentafel = new Buchstabentafel();

        // Act
        buchstabentafel.addWord(Buchstabentafel.Words.albert);

        // Assert
        assertEquals(1, buchstabentafel.str.size());
        assertEquals("albert", buchstabentafel.getListOfWords().get(0));
    }

     */
}